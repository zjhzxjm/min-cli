# min plugin-scaffold

## 安装

``` bash
$ npm install @mindev/min-plugin-scaffold --save-dev
```

## 配置 `min.config.js`

``` js
module.exports = {
  plugins: {
    scaffold: {
      filter: /\.wxss$/,
      config: {}
    }
  }
}
```

## 参数说明
