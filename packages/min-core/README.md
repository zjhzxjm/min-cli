# min core

## 安装

``` bash
$ npm install @mindev/min-core --save-dev
```

## 配置 `min.config.js`

``` js
module.exports = {
  plugins: {
    core: {
      filter: /\.wxss$/,
      config: {}
    }
  }
}
```

## 参数说明
